server:
  port: 8084

spring:
  application:
    name: autopenttest-service

  datasource:
    url: ${POSTGRES_URL:jdbc:postgresql://localhost:5432/iacyber}
    username: ${POSTGRES_USER:iacyber}
    password: ${POSTGRES_PASSWORD:iacyber_dev}

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: all

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_URL:http://localhost:8180}/realms/iacyber

iacyber:
  ai:
    pentest-api-url: ${PENTEST_AI_API_URL:http://localhost:9007}
    internal-token: ${INTERNAL_TOKEN:iacyber-internal-dev-token}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

logging:
  level:
    com.iacyber: DEBUG

---
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:postgresql://postgres:5432/iacyber
  kafka:
    bootstrap-servers: kafka:29092
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8080/realms/iacyber
